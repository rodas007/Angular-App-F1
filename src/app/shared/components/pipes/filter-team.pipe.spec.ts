/* tslint:disable:no-unused-variable */


import { FilterTeamPipe } from './filter-team.pipe';

describe('Pipe: teamsfilter', () => {
  it('create an instance', () => {
    let pipe = new FilterTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
